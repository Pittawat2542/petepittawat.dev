---
import type { HTMLAttributes } from 'astro/types';
interface Props extends HTMLAttributes<'section'> {
  class?: string;
}
const { class: className, ...rest } = Astro.props as Props;
---
<section {...rest} class={`glass-card p-6 md:p-8 ${className ?? ''}`}>
  <slot />
</section>
