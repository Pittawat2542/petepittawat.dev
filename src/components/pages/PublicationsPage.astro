---
import PageWithHero from '@/layouts/PageWithHero.astro';
import { SITE_TITLE, SITE_DESCRIPTION } from '@/consts';
import { PublicationsExplorer } from '@/components/explorers';
import academicServices from '@/data/services/academic.json';
import AcademicServices from '@/components/sections/AcademicServices';
import { getLangFromUrl, getSiteCopy } from '@/i18n/utils';
import { getPublications } from '@/lib/content';

const lang = getLangFromUrl(Astro.url);
const siteCopy = getSiteCopy(lang);
const pageCopy = siteCopy.listingPages.publications;
const publications = await getPublications();
---

<PageWithHero
  title={`${SITE_TITLE} - ${pageCopy.title}`}
  description={pageCopy.metaDescription || SITE_DESCRIPTION}
  hero={{
    title: pageCopy.title,
    subtitle: pageCopy.heroSubtitle,
    page: 'publications',
  }}
  containerClass="glass-section"
>
  <PublicationsExplorer client:visible items={publications} />
  <AcademicServices
    client:visible
    data={academicServices}
    title={pageCopy.academicServicesTitle}
    description={pageCopy.academicServicesDescription}
  />
</PageWithHero>
